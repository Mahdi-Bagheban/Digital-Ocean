# بسم الله الرحمن الرحیم

# Digital-Ocean Dev Workspace 🚀

یک محیط توسعه‌ی موقت، سریع و تمیز روی **DigitalOcean** برای کارهای سنگین، تست، توسعه و بازی با تکنولوژی‌ها.

این مخزن به شما کمک می‌کند با چند کلیک از منوی **GitHub Actions**:

1. **سرور Memory-Optimized با 32GB RAM** بسازید 🧠
2. روی آن **KASM Workspace (محیط دسکتاپ روی مرورگر)** بالا بیاید 🖥️
3. نرم‌افزارهای پایه‌‌ی توسعه نصب شوند (Node.js, Python, Docker, Git, ...)
4. بعد از اتمام کار، سرور را کامل **حذف کنید تا هزینه‌ای شارژ نشود** 💸

---

## 🧱 مشخصات سرور

- **Provider:** DigitalOcean
- **Plan:** Memory-Optimized – 2 vCPU, 32GB RAM, 100GB SSD
- **OS:** Ubuntu 22.04 LTS
- **Networking:**
  - IPv4 Public
  - IPv6 فعال
- **Monitoring:** فعال ✅
- **Tags:** `mahdiarts`, `kasm-workspace`, `development`

### 🧩 نرم‌افزارهای نصب‌شده به‌صورت خودکار

- Docker 🐳 (برای KASM و سرویس‌ها)
- KASM Workspace (محیط دسکتاپ کامل در مرورگر)
- Node.js 20
- Python 3 + pip
- Git, build-essential, curl, wget, htop, tmux و ابزارهای پایه توسعه

> نکته: نرم‌افزارهایی مثل VS Code, Chrome, Android Studio, CapCut, Canva و ... را می‌توانید داخل محیط KASM و مرورگر نصب/استفاده کنید.

---

## ⚙️ پیش‌نیازها

قبل از استفاده از این مخزن، موارد زیر لازم است:

1. حساب DigitalOcean با **API Token** فعال
2. ساخت یک **SSH Key** در DigitalOcean (مثلاً با نام `MahdiArts`)
3. تنظیم دو **GitHub Secret** در مخزن:

در تنظیمات ریپو:
`Settings → Secrets and variables → Actions → New repository secret`

- `DO_API_TOKEN` → توکن API از DigitalOcean
- `SSH_KEY_NAME` → نام کلید SSH (مثلاً `MahdiArts`)

> فایل `.env.example` فقط نمونه است. در GitHub Actions نیازی به ساخت `.env` نیست، خود Workflow این کار را انجام می‌دهد.

---

## 🚀 ایجاد سرور از طریق GitHub Actions

1. وارد تب **Actions** در ریپو شوید.
2. Workflow با نام **"🚀 Create Development Server with KASM"** را انتخاب کنید.
3. روی **Run workflow** کلیک کنید.
4. اگر خواستید:
   - `server_name` → نام سرور (مثلاً: `mahdi-dev-workspace`)
   - `region` → یکی از مناطق: `fra1`, `ams3`, `lon1`, `nyc1`
5. چند دقیقه صبر کنید تا Job تمام شود.

در پایان، در بخش **Run summary** یک بخش **Summary** خواهید دید که شامل:

- Server ID
- Server IP
- Region
- مشخصات سرور
- دستور اتصال SSH
- آدرس KASM Workspace

### 🧑‍💻 اتصال به سرور

**SSH:**
```bash
ssh root@YOUR_SERVER_IP
```

**KASM Workspace (دسکتاپ در مرورگر):**
```text
https://YOUR_SERVER_IP:443
```

> توصیه می‌شود روی مرورگر، هشدار SSL را برای تست نادیده بگیرید (Self-Signed) یا بعداً SSL معتبر تنظیم کنید.

---

## 🗑️ حذف سرور و جلوگیری از هزینه اضافه

هر زمان کارت تمام شد، حتماً سرور را حذف کن تا هزینه اضافه نخوری 💸

1. وارد تب **Actions** در ریپو شوید.
2. Workflow با نام **"🗑️ Delete Development Server"** را انتخاب کنید.
3. روی **Run workflow** کلیک کنید.
4. در فیلد `confirm` مقدار **`yes`** را انتخاب کنید.
5. Workflow اجرا شده و سرور حذف می‌شود.

در Summary مربوط به حذف، پیامی مبنی بر موفقیت‌آمیز بودن حذف و توضیح کوتاه درباره هزینه نمایش داده می‌شود.

---

## 🧪 اجرای دستی اسکریپت‌ها (اختیاری)

اگر خواستی به‌جای GitHub Actions، مستقیم روی سیستم خودت اسکریپت‌ها را اجرا کنی:

### 1️⃣ کلون مخزن
```bash
git clone https://github.com/Mahdi-Bagheban/Digital-Ocean.git
cd Digital-Ocean
```

### 2️⃣ ساخت فایل `.env`

فایل `.env.example` را کپی کن:
```bash
cp .env.example .env
```

و مقادیر داخلش را با توکن واقعی و نام SSH Key عوض کن.

### 3️⃣ اجرای اسکریپت ایجاد سرور

```bash
chmod +x create-server.sh
./create-server.sh
```

### 4️⃣ اجرای اسکریپت حذف سرور

```bash
chmod +x delete-server.sh
./delete-server.sh
```

---

## 🔍 نکات مهم و توصیه‌ها

- این سرور برای **کار موقت، تست و توسعه** طراحی شده، نه برای پروداکشن بلندمدت.
- هزینه حدودی:
  - ~250$ در ماه → حدود 0.347$ در ساعت
  - یعنی اگر 3 ساعت استفاده کنی ≈ حدود 1$ هزینه
- قبل از بستن لپ‌تاپ یا خوابیدن 😄 حتماً از Workflow حذف استفاده کن.

---

## ✅ چک‌لیست صحت پروژه

- [x] اسکریپت `create-server.sh` آپدیت شد و KASM + نرم‌افزارهای پایه را نصب می‌کند.
- [x] اسکریپت `delete-droplet.sh` به `delete-server.sh` تغییر نام و بهبود داده شد.
- [x] Workflow ایجاد سرور (`create-server.yml`) با ورودی‌های ساده از منوی Actions تنظیم شد.
- [x] Workflow حذف سرور (`delete-server.yml`) با تأیید `yes` و اجرای خودکار تنظیم شد.
- [x] README با توضیحات ساده، زیبا و کامل نوشته شد.

---

اگر دوست داشتی، می‌توانیم در قدم بعد:

- نصب خودکار VS Code Server, Chrome, Android SDK و ابزارهای دیگر را داخل KASM بیشتر اتوماتیک کنیم.
- اسکریپت جدا برای **مانیتورینگ هزینه و زمان استفاده** بنویسیم.
- پروفایل‌های آماده برای Workspace (مثل Node.js Dev, Android Dev, AI/ML Dev) بسازیم.


