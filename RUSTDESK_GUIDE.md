# 🔐 راهنمای کامل RustDesk Server

## 📖 فهرست مطالب

- [مقدمه](#مقدمه)
- [نصب خودکار](#نصب-خودکار)
- [پیکربندی کلاینت](#پیکربندی-کلاینت)
- [نحوه استفاده](#نحوه-استفاده)
- [عیب‌یابی](#عیبیابی)
- [نکات امنیتی](#نکات-امنیتی)

---

## مقدمه

### 🤔 RustDesk چیست؟

**RustDesk** یک نرم‌افزار متن‌باز برای دسترسی از راه دور (Remote Desktop) است که جایگزینی قدرتمند برای TeamViewer و AnyDesk محسوب می‌شود.

### ✨ ویژگی‌اا

- ✅ **رایگان و متن‌باز**: بدون محدودیت استفاده کنید
- 🔒 **امنیت بالا**: رمزنگاری End-to-End
- 🚀 **سریع و سبک**: پروتکل بهینه شده
- 🌐 **چند پلتفرمی**: Windows, macOS, Linux, iOS, Android
- 🛡️ **سرور شخصی**: کنترل کامل در دست شما

### 🏛️ معماری RustDesk

RustDesk از دو جزء اصلی تشکیل شده است:

1. **hbbs (Hub Server)** در پورت‌های:
   - `21115` - TCP: سرویس NAT traversal
   - `21116` - TCP/UDP: سرویس ID registration و heartbeat
   - `21118` - TCP: وب کنسول مدیریت

2. **hbbr (Relay Server)** در پورت‌های:
   - `21117` - TCP: سرویس relay
   - `21119` - TCP: سرویس وبسوکت

---

## نصب خودکار

### ✅ نصب از طریق اسکریپت

زمانی که از `create-server.sh` استفاده می‌کنید، **RustDesk Server به صورت خودکار نصب می‌شود**.

```bash
# اجرای اسکریپت ایجاد سرور
./create-server.sh
```

بعد از اتمام نصب (حدود ۵-۱۵ دقیقه)، برای دریافت کلید عمومی:

```bash
ssh root@YOUR_SERVER_IP cat /root/rustdesk-public-key.txt
```

### 🔍 بررسی وضعیت نصب

برای بررسی وضعیت RustDesk Server:

```bash
# اتصال به سرور
ssh root@YOUR_SERVER_IP

# بررسی وضعیت Containerها
cd /opt/rustdesk
docker-compose ps

# مشاهده لاگ‌ها
docker-compose logs -f

# مشاهده لاگ فقط hbbs
docker-compose logs -f hbbs

# مشاهده لاگ فقط hbbr
docker-compose logs -f hbbr
```

### 🔄 ریستارت RustDesk Server

```bash
cd /opt/rustdesk

# ریستارت تمام سرویس‌ها
docker-compose restart

# ریستارت فقط hbbs
docker-compose restart hbbs

# ریستارت فقط hbbr
docker-compose restart hbbr
```

### 🛁 خاموش کردن RustDesk Server

```bash
cd /opt/rustdesk
docker-compose down
```

### ▶️ روشن کردن RustDesk Server

```bash
cd /opt/rustdesk
docker-compose up -d
```

---

## پیکربندی کلاینت

### 📥 دانلود کلاینت RustDesk

برای دستگاه‌های خود از سایت رسمی دانلود کنید:

🔗 **[https://rustdesk.com/](https://rustdesk.com/)**

### ⚙️ پیکربندی کلاینت

#### 💻 Windows / macOS / Linux

1. **باز کردن RustDesk**
   - برنامه را اجرا کنید

2. **ورود به تنظیمات**
   - روی آیکون `⋮` (سه نقطه عمودی) کلیک کنید
   - `Settings` یا `ID/Relay Server` را انتخاب کنید

3. **تنظیم سرور شخصی**
   
   در بخش **ID Server**:
   ```
   ID Server: YOUR_SERVER_IP
   ```

   در بخش **Relay Server** (اختیاری):
   ```
   Relay Server: YOUR_SERVER_IP
   ```

   در بخش **Key** (مهم!):
   ```
   Key: YOUR_PUBLIC_KEY
   ```
   > 📝 **نکته**: کلید عمومی را از `/root/rustdesk-public-key.txt` کپی کنید

4. **ذخیره تنظیمات**
   - روی `Apply` یا `OK` کلیک کنید

5. **ریستارت RustDesk**
   - برنامه را ببندید و دوباره باز کنید

#### 📱 Android / iOS

1. **نصب اپلیکیشن**
   - Android: از Google Play یا F-Droid
   - iOS: از App Store

2. **باز کردن تنظیمات**
   - روی آیکون تنظیمات (چرخ دنده) کلیک کنید
   - `ID/Relay Server` را انتخاب کنید

3. **ورود اطلاعات**
   - مشابه بخش دسکتاپ

### ✅ تایید پیکربندی

بعد از پیکربندی، در پنجره اصلی RustDesk:

- باید یک **ID عددی** مشاهده کنید (مثل `123456789`)
- در پایین باید عبارت **"Ready"** یا **"آماده"** نمایش داده شود

🎉 **تبریک! پیکربندی با موفقیت انجام شد.**

---

## نحوه استفاده

### 🔗 اتصال به کامپیوتر دیگر

#### روش ۱: اتصال مستقیم

1. **در دستگاه مقصد** (Remote PC):
   - RustDesk را باز کنید
   - **ID** دستگاه را یادداشت کنید
   - یک **رمز دسترسی موقت** تنظیم کنید (اختیاری)

2. **در دستگاه خود** (Controller):
   - RustDesk را باز کنید
   - **ID** دستگاه مقصد را وارد کنید
   - روی `Connect` کلیک کنید
   - رمز دسترسی را وارد کنید

#### روش ۲: رمز دائمی (توصیه می‌شود)

برای دسترسی با رمز ثابت:

1. **در دستگاه مقصد**:
   - RustDesk را باز کنید
   - روی آیکون `⋮` کلیک کرده و `Settings` را انتخاب کنید
   - به بخش `Security` بروید
   - `Permanent Password` را فعال کرده و رمز دلخواه تنظیم کنید

2. **حالا می‌توانید با رمز ثابت اتصال برقرار کنید**

### 📷 قابلیت‌های اضافی

- 📎 **انتقال فایل**: Drag & Drop فایل‌ها
- 📋 **Clipboard Sync**: کپی/پیست بین دستگاه‌ها
- 🔊 **انتقال صدا**: شنیدن صدای دستگاه مقصد
- 🖊️ **چند مانیتور**: انتخاب مانیتورهای مختلف
- ⌨️ **Keyboard Shortcuts**: میانبرهای صفحه‌کلیدی
- 🔒 **قفل صفحه**: قفل دستگاه مقصد

---

## عیبیابی

### ❌ مشکل: نمی‌توانم به سرور متصل شوم

**علل محتمل:**

1. **Firewall بلاک کرده**
   ```bash
   # بررسی Firewall
   sudo ufw status
   
   # باز کردن پورت‌ها
   sudo ufw allow 21115:21119/tcp
   ```

2. **سرور خاموش است**
   ```bash
   cd /opt/rustdesk
   docker-compose ps
   
   # اگر خاموش بود
   docker-compose up -d
   ```

3. **IP اشتباه است**
   ```bash
   curl ifconfig.me
   ```

### ❌ مشکل: کلید عمومی کار نمی‌کند

**حل:**

```bash
# دریافت مجدد کلید
cd /opt/rustdesk
cat data/id_ed25519.pub

# اگر فایل وجود ندارد، سرور را ریستارت کنید
docker-compose restart
sleep 5
cat data/id_ed25519.pub
```

### ❌ مشکل: اتصال بسیار کند است

**علل محتمل:**

1. **تنظیمات کیفیت تصویر**
   - در RustDesk کیفیت تصویر را کاهش دهید
   - `Settings` > `Display` > `Quality`

2. **اتصال اینترنت ضعیف**
   - بررسی سرعت اینترنت خود

3. **فاصله جغرافیایی زیاد**
   - سرور را در منطقه نزدیک‌تر ایجاد کنید

### 📊 بررسی لاگ‌ها

```bash
# لاگ زنده (Live)
cd /opt/rustdesk
docker-compose logs -f

# لاگ 50 خط آخر
docker-compose logs --tail=50

# لاگ با تایم استمپ
docker-compose logs -t
```

---

## نکات امنیتی

### 🔒 توصیه‌های امنیتی

1. **استفاده از کلید عمومی**
   - حتماً کلید عمومی را در کلاینت‌ها وارد کنید
   - بدون کلید، هر کسی می‌تواند به سرور متصل شود

2. **رمزهای قوی**
   - از رمزهای پیچیده و طولانی استفاده کنید
   - حداقل ۱۲ کاراکتر با ترکیب عدد، حرف و علامت

3. **محدود کردن دسترسی**
   ```bash
   # محدود کردن به IP خاص
   sudo ufw allow from YOUR_IP to any port 21115:21119 proto tcp
   ```

4. **بروزرسانی منظم**
   ```bash
   # بروزرسانی Docker Images
   cd /opt/rustdesk
   docker-compose pull
   docker-compose up -d
   ```

5. **بکاپ کلید خصوصی**
   ```bash
   # بکاپ کلیدها
   scp root@YOUR_SERVER_IP:/opt/rustdesk/data/id_ed25519* ./backup/
   ```

### 🛡️ Firewall پیشرفته

برای امنیت بیشتر:

```bash
# فقط اجازه SSH و RustDesk
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 22/tcp
sudo ufw allow 21115:21119/tcp
sudo ufw enable

# مشاهده وضعیت
sudo ufw status verbose
```

### 📊 مانیتورینگ

```bash
# مشاهده اتصالات فعال
netstat -tulpn | grep -E '2111[5-9]'

# مشاهده مصرف منابع
docker stats

# بررسی دیسک
df -h /opt/rustdesk
```

---

## 📚 منابع بیشتر

- 🌐 **سایت رسمی**: [https://rustdesk.com/](https://rustdesk.com/)
- 💻 **مخزن GitHub**: [https://github.com/rustdesk/rustdesk](https://github.com/rustdesk/rustdesk)
- 📖 **مستندات**: [https://rustdesk.com/docs/](https://rustdesk.com/docs/)
- 💬 **انجمن**: [https://github.com/rustdesk/rustdesk/discussions](https://github.com/rustdesk/rustdesk/discussions)

---

## 🌟 خلاصه

با این راهنما:

✅ RustDesk Server به صورت خودکار نصب می‌شود  
✅ کلاینت‌ها را به راحتی پیکربندی کردید  
✅ امنیت سرور خود را تضمین کردید  
✅ مشکلات رایج را حل کردید  

🎉 **حالا می‌توانید به راحتی به سیستم‌های خود از راه دور دسترسی داشته باشید!**

---

👨‍💻 **تهیه شده توسط**: Mahdi Bagheban  
📅 **تاریخ**: دسامبر 2025  
📦 **نسخه**: 1.0
