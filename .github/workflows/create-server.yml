name: 🚀 Create Development Server

on:
  workflow_dispatch:
    inputs:
      server_name:
        description: '💻 Server Name (نام سرور)'
        required: false
        default: 'mahdi-dev-workspace'
      region:
        description: '🗺️ Datacenter Region (منطقه دیتاسنتر)'
        required: false
        default: 'fra1'
        type: choice
        options:
          - fra1  # Frankfurt (فرانکفورت)
          - ams3  # Amsterdam (آمستردام)
          - lon1  # London (لندن)
          - nyc1  # New York (نیویورک)
          - sgp1  # Singapore (سنگاپور)

jobs:
  create-server:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: 🔐 Validate Configuration
        id: validate
        run: |
          echo "🔍 Validating secrets and configuration..."
          
          # Check DO_API_TOKEN
          if [ -z "${{ secrets.DO_API_TOKEN }}" ]; then
            echo "❌ DO_API_TOKEN is not configured!"
            echo "::error::Please configure DO_API_TOKEN in repository secrets"
            exit 1
          fi
          
          # Check SSH_KEY_NAME
          if [ -z "${{ secrets.SSH_KEY_NAME }}" ]; then
            echo "❌ SSH_KEY_NAME is not configured!"
            echo "::error::Please configure SSH_KEY_NAME in repository secrets"
            exit 1
          fi
          
          echo "✅ All secrets are properly configured"
          echo "server_name=${{ github.event.inputs.server_name }}" >> $GITHUB_OUTPUT
          echo "region=${{ github.event.inputs.region }}" >> $GITHUB_OUTPUT
      
      - name: 🔑 Get SSH Key ID
        id: ssh_key
        run: |
          echo "🔍 Fetching SSH key information..."
          
          response=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.DO_API_TOKEN }}" \
            "https://api.digitalocean.com/v2/account/keys")
          
          # Check for API errors
          if echo "$response" | jq -e '.id' > /dev/null 2>&1; then
            error_msg=$(echo "$response" | jq -r '.message')
            echo "❌ API Error: $error_msg"
            echo "::error::DigitalOcean API Error: $error_msg"
            exit 1
          fi
          
          # Find SSH key by name
          ssh_key_id=$(echo "$response" | jq -r ".ssh_keys[] | select(.name==\"${{ secrets.SSH_KEY_NAME }}\") | .id")
          
          if [ -z "$ssh_key_id" ] || [ "$ssh_key_id" = "null" ]; then
            echo "❌ SSH Key '${{ secrets.SSH_KEY_NAME }}' not found!"
            echo "Available SSH keys:"
            echo "$response" | jq -r '.ssh_keys[] | "  - \(.name) (ID: \(.id))"'
            echo "::error::SSH key not found. Please check SSH_KEY_NAME secret"
            exit 1
          fi
          
          echo "✅ SSH Key found: ${{ secrets.SSH_KEY_NAME }} (ID: $ssh_key_id)"
          echo "ssh_key_id=$ssh_key_id" >> $GITHUB_OUTPUT
      
      - name: 📝 Prepare Cloud-Init Script
        id: cloud_init
        run: |
          echo "📝 Creating cloud-init user data script..."
          
          # Create optimized cloud-init script
          cat > user-data.sh << 'EOF'
#!/bin/bash
set -e

# Setup logging
exec > >(tee -a /var/log/cloud-init-custom.log)
exec 2>&1

echo "=== Starting server setup at $(date) ==="

# Update system
export DEBIAN_FRONTEND=noninteractive
apt-get update -qq
apt-get upgrade -y -qq

# Install essential packages
apt-get install -y -qq \
  curl wget git vim nano \
  htop tmux net-tools \
  build-essential ca-certificates \
  jq unzip software-properties-common

# Install Docker
echo "Installing Docker..."
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
usermod -aG docker root
systemctl enable docker
systemctl start docker

# Install Node.js 20
echo "Installing Node.js..."
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y -qq nodejs

# Install Python
echo "Installing Python..."
apt-get install -y -qq python3 python3-pip python3-venv
pip3 install --upgrade pip setuptools wheel

# Install KASM Workspace (in background)
echo "Installing KASM Workspace..."
(
  cd /tmp
  wget -q https://kasm-static-content.s3.amazonaws.com/kasm_release_1.15.0.5b7fb6.tar.gz
  tar -xzf kasm_release_1.15.0.5b7fb6.tar.gz
  cd kasm_release
  bash install.sh -L -e -m 32 2>&1 | tee /var/log/kasm-install.log
) &

# Create welcome message
cat > /etc/motd << 'MOTD'
╔══════════════════════════════════════════════════════════╗
║     🚀 Mahdi's Development Server - Ready to Code! 🚀    ║
╚══════════════════════════════════════════════════════════╝

📦 Installed Software:
  ✅ Docker + Docker Compose
  ✅ Node.js $(node --version 2>/dev/null || echo 'installing...')
  ✅ Python $(python3 --version 2>/dev/null || echo 'installing...')
  ✅ Git & Development Tools
  ✅ KASM Workspace (check: tail -f /var/log/kasm-install.log)

🌐 Quick Commands:
  docker ps              - View running containers
  systemctl status kasm  - Check KASM status  
  htop                   - System monitor

MOTD

echo "=== Setup completed at $(date) ==="
echo "✅ All packages installed successfully"
EOF

          # Encode to base64 (works on both Linux and macOS)
          if base64 --version 2>&1 | grep -q "GNU"; then
            # GNU base64 (Linux)
            USER_DATA_B64=$(cat user-data.sh | base64 -w 0)
          else
            # BSD base64 (macOS)
            USER_DATA_B64=$(cat user-data.sh | base64)
          fi
          
          echo "user_data=$USER_DATA_B64" >> $GITHUB_OUTPUT
          echo "✅ Cloud-init script prepared"
      
      - name: 🏗️ Create Droplet
        id: create
        run: |
          echo "🏗️ Creating droplet..."
          echo "Name: ${{ github.event.inputs.server_name }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Size: m-2vcpu-32gb (32GB RAM, 2 vCPU)"
          echo "Image: Ubuntu 22.04 LTS"
          echo ""
          
          # Create JSON payload
          cat > payload.json << EOF
{
  "name": "${{ github.event.inputs.server_name }}",
  "region": "${{ github.event.inputs.region }}",
  "size": "m-2vcpu-32gb",
  "image": "ubuntu-22-04-x64",
  "ssh_keys": [${{ steps.ssh_key.outputs.ssh_key_id }}],
  "backups": false,
  "ipv6": true,
  "monitoring": true,
  "tags": ["mahdiarts", "development", "github-actions"],
  "user_data": "${{ steps.cloud_init.outputs.user_data }}"
}
EOF
          
          # Send create request
          response=$(curl -s -w "\n%{http_code}" -X POST \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.DO_API_TOKEN }}" \
            -d @payload.json \
            "https://api.digitalocean.com/v2/droplets")
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          # Check HTTP status
          if [ "$http_code" != "202" ] && [ "$http_code" != "201" ]; then
            echo "❌ API request failed with HTTP $http_code"
            echo "$body" | jq '.'
            error_msg=$(echo "$body" | jq -r '.message // "Unknown error"')
            echo "::error::$error_msg"
            exit 1
          fi
          
          # Extract droplet ID
          droplet_id=$(echo "$body" | jq -r '.droplet.id')
          
          if [ -z "$droplet_id" ] || [ "$droplet_id" = "null" ]; then
            echo "❌ Failed to get droplet ID"
            echo "$body" | jq '.'
            exit 1
          fi
          
          echo "✅ Droplet created successfully!"
          echo "Droplet ID: $droplet_id"
          echo "droplet_id=$droplet_id" >> $GITHUB_OUTPUT
          echo "$droplet_id" > droplet_id.txt
      
      - name: ⏳ Wait for Droplet to be Active
        id: wait
        run: |
          droplet_id="${{ steps.create.outputs.droplet_id }}"
          echo "⏳ Waiting for droplet to become active..."
          echo ""
          
          max_attempts=60
          attempt=0
          status="new"
          
          while [ "$status" != "active" ] && [ $attempt -lt $max_attempts ]; do
            sleep 10
            attempt=$((attempt + 1))
            
            response=$(curl -s -X GET \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.DO_API_TOKEN }}" \
              "https://api.digitalocean.com/v2/droplets/$droplet_id")
            
            status=$(echo "$response" | jq -r '.droplet.status')
            
            progress=$((attempt * 100 / max_attempts))
            echo "[⏳] Status: $status | Progress: ${progress}% | Attempt: ${attempt}/${max_attempts}"
          done
          
          echo ""
          
          if [ "$status" != "active" ]; then
            echo "❌ Droplet did not become active in time (10 minutes)"
            echo "Current status: $status"
            echo "::error::Timeout waiting for droplet activation"
            exit 1
          fi
          
          echo "✅ Droplet is now active!"
      
      - name: 📡 Get Droplet Information
        id: info
        run: |
          droplet_id="${{ steps.create.outputs.droplet_id }}"
          echo "📡 Fetching droplet information..."
          
          response=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.DO_API_TOKEN }}" \
            "https://api.digitalocean.com/v2/droplets/$droplet_id")
          
          # Extract information
          droplet_ip=$(echo "$response" | jq -r '.droplet.networks.v4[0].ip_address')
          droplet_ipv6=$(echo "$response" | jq -r '.droplet.networks.v6[0].ip_address // "N/A"')
          droplet_status=$(echo "$response" | jq -r '.droplet.status')
          droplet_created=$(echo "$response" | jq -r '.droplet.created_at')
          
          if [ -z "$droplet_ip" ] || [ "$droplet_ip" = "null" ]; then
            echo "❌ Failed to get droplet IP address"
            exit 1
          fi
          
          echo "✅ Droplet information retrieved"
          echo "IPv4: $droplet_ip"
          echo "IPv6: $droplet_ipv6"
          echo "Status: $droplet_status"
          echo "Created: $droplet_created"
          
          # Save to outputs and files
          echo "droplet_ip=$droplet_ip" >> $GITHUB_OUTPUT
          echo "droplet_ipv6=$droplet_ipv6" >> $GITHUB_OUTPUT
          echo "droplet_created=$droplet_created" >> $GITHUB_OUTPUT
          
          echo "$droplet_ip" > droplet_ip.txt
          echo "$droplet_created" > droplet_created.txt
      
      - name: 📤 Save Server Information
        uses: actions/upload-artifact@v4
        with:
          name: server-info
          path: |
            droplet_id.txt
            droplet_ip.txt
            droplet_created.txt
          retention-days: 7
      
      - name: 🎉 Success Summary
        if: success()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
## 🎉 سرور با موفقیت ایجاد شد!

### 📋 اطلاعات سرور

| مورد | مقدار |
|------|-------|
| **🆔 Server ID** | `${{ steps.create.outputs.droplet_id }}` |
| **🌐 IPv4 Address** | `${{ steps.info.outputs.droplet_ip }}` |
| **📍 IPv6 Address** | `${{ steps.info.outputs.droplet_ipv6 }}` |
| **💻 Server Name** | `${{ github.event.inputs.server_name }}` |
| **🗺️ Region** | `${{ github.event.inputs.region }}` |
| **💾 RAM** | 32GB |
| **🔧 CPU** | 2 vCPU |
| **💿 Storage** | 100GB SSD |
| **🖥️ OS** | Ubuntu 22.04 LTS |
| **📅 Created** | `${{ steps.info.outputs.droplet_created }}` |

---

### 🔐 دسترسی به سرور

#### SSH Connection
```bash
ssh root@${{ steps.info.outputs.droplet_ip }}
```

#### KASM Workspace (Desktop در Browser)
```
https://${{ steps.info.outputs.droplet_ip }}:443
Username: admin@kasm.local
```
> ⚠️ **توجه:** نصب KASM حدود 10-15 دقیقه زمان می‌برد

---

### 📦 نرم‌افزارهای نصب شده

- ✅ **Docker** + Docker Compose
- ✅ **Node.js 20** + npm
- ✅ **Python 3** + pip
- ✅ **Git** & Development Tools
- ✅ **KASM Workspace** (در حال نصب)

---

### 🔍 بررسی وضعیت نصب

برای بررسی پیشرفت نصب KASM:
```bash
ssh root@${{ steps.info.outputs.droplet_ip }} "tail -f /var/log/kasm-install.log"
```

برای بررسی وضعیت سرویس KASM:
```bash
ssh root@${{ steps.info.outputs.droplet_ip }} "systemctl status kasm"
```

---

### 💰 هزینه

**Memory-Optimized 32GB:** $250/ماه ($0.347/ساعت)

> 💡 برای جلوگیری از هزینه اضافی، حتماً پس از اتمام کار سرور را حذف کنید!

---

### 🗑️ حذف سرور

برای حذف سرور از workflow **Delete Server** استفاده کنید:
1. به تب [Actions](../../actions) بروید
2. workflow **🗑️ Delete Development Server** را انتخاب کنید
3. **Run workflow** را کلیک کنید
4. گزینه `yes` را انتخاب کنید

---

### 📚 منابع مفید

- [DigitalOcean Dashboard](https://cloud.digitalocean.com/droplets)
- [KASM Documentation](https://kasmweb.com/docs/latest/index.html)
- [Repository](${{ github.server_url }}/${{ github.repository }})

---

✨ **سرور شما آماده است! موفق باشید!** ✨
EOF
      
      - name: ❌ Failure Summary
        if: failure()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
## ❌ خطا در ایجاد سرور

متأسفانه در فرآیند ایجاد سرور خطایی رخ داد.

### 🔍 راهنمای عیب‌یابی

#### 1️⃣ بررسی Secrets
مسیر: `Settings` → `Secrets and variables` → `Actions`

باید این secrets تنظیم شده باشند:
- ✅ `DO_API_TOKEN` - توکن API از DigitalOcean
- ✅ `SSH_KEY_NAME` - نام کلید SSH در DigitalOcean

#### 2️⃣ بررسی توکن API
توکن باید دسترسی‌های زیر را داشته باشد:
- ✅ Read & Write Droplets
- ✅ Read SSH Keys
- ✅ Read Account Info

برای ایجاد توکن جدید:
1. به [DigitalOcean API Tokens](https://cloud.digitalocean.com/account/api/tokens) بروید
2. **Generate New Token** را کلیک کنید
3. نام را وارد کنید و **Write** را فعال کنید

#### 3️⃣ بررسی کلید SSH
کلید SSH باید در DigitalOcean موجود باشد:
1. به [SSH Keys](https://cloud.digitalocean.com/account/security) بروید
2. اطمینان حاصل کنید کلید با نام `${{ secrets.SSH_KEY_NAME }}` وجود دارد

#### 4️⃣ بررسی لاگ‌های خطا
برای جزئیات بیشتر:
- لاگ‌های کامل را در تب **Actions** مشاهده کنید
- به قسمت failed step مراجعه کنید

#### 5️⃣ بررسی محدودیت‌های اکانت
- آیا اکانت DigitalOcean شما فعال است؟
- آیا به حد مجاز droplet رسیده‌اید؟
- آیا کارت اعتباری معتبر متصل است؟

---

### 📞 دریافت کمک

اگر مشکل ادامه دارد:
1. لاگ‌های خطا را کپی کنید
2. یک [Issue جدید](../../issues/new) ایجاد کنید
3. لاگ‌ها و جزئیات را در issue قرار دهید

---

### 🔄 تلاش مجدد

پس از رفع مشکلات، می‌توانید workflow را دوباره اجرا کنید:
1. به تب [Actions](../../actions) بروید
2. این workflow را انتخاب کنید
3. **Re-run jobs** را کلیک کنید

---

**Run ID:** `${{ github.run_id }}`  
**Job ID:** `${{ github.job }}`  
**Timestamp:** `$(date -u +'%Y-%m-%dT%H:%M:%SZ')`
EOF
