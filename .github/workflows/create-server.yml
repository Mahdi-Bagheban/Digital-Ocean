# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 DigitalOcean Server Automation Workflow
# ═══════════════════════════════════════════════════════════════════════════════
#
# 📝 Purpose: اتوماسیون کامل ایجاد و مدیریت سرورهای DigitalOcean
# 🎯 Features: KASM Workspace + RustDesk Server + Full Automation
# 👤 Author: Mahdi Bagheban
# 📅 Updated: December 2025
# 🔖 Version: 7.0 (Production Ready - دسته‌بندی شده: معمولی، قدرتمند، فوق‌العاده)
#
# ═══════════════════════════════════════════════════════════════════════════════

name: 🚀 ایجاد سرور DigitalOcean | Create Server

on:
  workflow_dispatch:
    inputs:
      # ════════════════════════════════════════════════════════════════════
      # 📝 بخش اول: مشخصات اصلی سرور
      # ════════════════════════════════════════════════════════════════════
      
      server_name:
        description: '📝 نام سرور | Server Name'
        required: true
        default: 'mahdi-premium-server'
        type: string
      
      region:
        description: '🌍 منطقه جغرافیایی | Region'
        required: true
        default: 'fra1'
        type: choice
        options:
          - 'fra1'  # 🇩🇪 Frankfurt, Germany (Low Latency EU/ME)
          - 'ams3'  # 🇳🇱 Amsterdam, Netherlands  
          - 'lon1'  # 🇬🇧 London, UK
          - 'nyc1'  # 🇺🇸 New York, USA
          - 'nyc3'  # 🇺🇸 New York 3, USA
          - 'sfo2'  # 🇺🇸 San Francisco, USA
          - 'sfo3'  # 🇺🇸 San Francisco 3, USA
          - 'sgp1'  # 🇸🇬 Singapore (Low Latency Asia)
          - 'tor1'  # 🇨🇦 Toronto, Canada
          - 'blr1'  # 🇮🇳 Bangalore, India
      
      # ════════════════════════════════════════════════════════════════════
      # 💻 بخش دوم: انتخاب پلن سرور (سه دسته: معمولی، قدرتمند، فوق‌العاده)
      # ════════════════════════════════════════════════════════════════════
      
      size_slug:
        description: '💻 پلن سرور | Server Plan'
        required: true
        default: 's-2vcpu-4gb-intel'
        type: choice
        options:
          # ────────────────────────────────────────────────────────────────
          # 🟢 سرورهای معمولی | STANDARD SERVERS
          # مناسب برای: وب‌سایت، تست، توسعه، پروژه‌های کوچک
          # ────────────────────────────────────────────────────────────────
          
          - 's-1vcpu-1gb'           # 💰 $6/mo  | 1GB RAM, 1 vCPU, 25GB SSD | تست ساده
          - 's-1vcpu-2gb'           # 💰 $12/mo | 2GB RAM, 1 vCPU, 50GB SSD | وب کوچک
          - 's-2vcpu-2gb'           # 💰 $18/mo | 2GB RAM, 2 vCPUs, 60GB SSD | توسعه
          - 's-2vcpu-4gb'           # 💰 $24/mo | 4GB RAM, 2 vCPUs, 80GB SSD | پروداکشن کوچک
          
          # ⚡ Premium Intel (NVMe - سریعتر)
          - 's-1vcpu-1gb-intel'     # ⚡ $8/mo  | 1GB RAM, 1 Intel, 35GB NVMe | تست سریع
          - 's-1vcpu-2gb-intel'     # ⚡ $16/mo | 2GB RAM, 1 Intel, 70GB NVMe | وب سریع  
          - 's-2vcpu-2gb-intel'     # ⚡ $24/mo | 2GB RAM, 2 Intel, 90GB NVMe | توسعه سریع
          - 's-2vcpu-4gb-intel'     # ⚡ $32/mo | 4GB RAM, 2 Intel, 120GB NVMe | ⭐ RECOMMENDED
          - 's-4vcpu-8gb-intel'     # ⚡ $64/mo | 8GB RAM, 4 Intel, 240GB NVMe | پروداکشن متوسط
          
          # ────────────────────────────────────────────────────────────────
          # 🔵 سرورهای قدرتمند | POWERFUL SERVERS  
          # مناسب برای: اپلیکیشن‌های پرترافیک، دیتابیس، API سرور
          # ────────────────────────────────────────────────────────────────
          
          # CPU-Optimized (Dedicated CPU)
          - 'c-2'                   # 🚀 $42/mo  | 4GB RAM, 2 Dedicated CPUs, 25GB SSD
          - 'c-4'                   # 🚀 $84/mo  | 8GB RAM, 4 Dedicated CPUs, 50GB SSD
          - 'c-8'                   # 🚀 $168/mo | 16GB RAM, 8 Dedicated CPUs, 100GB SSD
          - 'c-16'                  # 🚀 $336/mo | 32GB RAM, 16 Dedicated CPUs, 200GB SSD
          
          # General Purpose (Dedicated - Balanced)
          - 'g-2vcpu-8gb-intel'     # 💎 $63/mo  | 8GB RAM, 2 Dedicated Intel, 50GB NVMe
          - 'g-4vcpu-16gb-intel'    # 💎 $126/mo | 16GB RAM, 4 Dedicated Intel, 100GB NVMe
          - 'g-8vcpu-32gb-intel'    # 💎 $252/mo | 32GB RAM, 8 Dedicated Intel, 200GB NVMe
          
          # ────────────────────────────────────────────────────────────────
          # 🔴 سرورهای فوق‌العاده | EXTREME SERVERS
          # مناسب برای: Big Data، ML/AI، سرورهای Game، ترافیک بالا
          # ────────────────────────────────────────────────────────────────
          
          # CPU-Optimized (Heavy Load)
          - 'c-32'                  # 💪 $672/mo  | 64GB RAM, 32 Dedicated CPUs, 400GB SSD
          - 'c-48'                  # 💪 $1008/mo | 96GB RAM, 48 Dedicated CPUs, 600GB SSD
          
          # General Purpose (Extreme Performance)
          - 'g-16vcpu-64gb-intel'   # 🏆 $504/mo  | 64GB RAM, 16 Dedicated Intel, 400GB NVMe
          - 'g-32vcpu-128gb-intel'  # 🏆 $1008/mo | 128GB RAM, 32 Dedicated Intel, 800GB NVMe
          - 'g-40vcpu-160gb-intel'  # 🏆 $1260/mo | 160GB RAM, 40 Dedicated Intel, 1TB NVMe
      
      # ════════════════════════════════════════════════════════════════════
      # 🔧 بخش سوم: تنظیمات پیشرفته
      # ════════════════════════════════════════════════════════════════════
      
      enable_ipv6:
        description: '🌐 فعال‌سازی IPv6'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      
      enable_backups:
        description: '💾 فعال‌سازی Backup خودکار (+20% هزینه)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      
      enable_monitoring:
        description: '📊 فعال‌سازی Monitoring (رایگان)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      
      custom_tags:
        description: '🏷️ برچسب‌های سفارشی (با کاما جدا کنید)'
        required: false
        default: 'mahdiarts,production,automated'
        type: string

# ═══════════════════════════════════════════════════════════════════════════════
# 🛠️ تعریف Job
# ═══════════════════════════════════════════════════════════════════════════════

jobs:
  create-server:
    name: 🏗️ ایجاد سرور
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      # ════════════════════════════════════════════════════════════════════
      # 📥 Step 1: دریافت کد
      # ════════════════════════════════════════════════════════════════════
      
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        
      # ════════════════════════════════════════════════════════════════════
      # 🔧 Step 2: نصب ابزارها
      # ════════════════════════════════════════════════════════════════════
      
      - name: 🔧 نصب Dependencies
        run: |
          echo "🔧 نصب ابزارهای مورد نیاز..."
          sudo apt-get update -qq
          sudo apt-get install -y jq curl bc figlet
          figlet "Mahdi Arts"
          echo "✅ ابزارها نصب شدند"
          
      # ════════════════════════════════════════════════════════════════════
      # 🔍 Step 3: بررسی کلیدهای API
      # ════════════════════════════════════════════════════════════════════
      
      - name: 🔍 Validate API Keys
        run: |
          echo "🔍 بررسی کلیدهای API..."
          
          if [ -z "${{ secrets.DO_API_TOKEN }}" ]; then
            echo "❌ خطا: DO_API_TOKEN یافت نشد!"
            exit 1
          fi
          
          if [ -z "${{ secrets.SSH_KEY_NAME }}" ]; then
            echo "❌ خطا: SSH_KEY_NAME یافت نشد!"
            exit 1
          fi
          
          echo "✅ تمام کلیدها معتبر هستند"
          
      # ════════════════════════════════════════════════════════════════════
      # 📊 Step 4: نمایش تنظیمات
      # ════════════════════════════════════════════════════════════════════
      
      - name: 📊 Display Configuration
        run: |
          echo "## 🎯 پیکربندی سرور" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 📌 پارامتر | 📝 مقدار |" >> $GITHUB_STEP_SUMMARY
          echo "|------------|-----------|" >> $GITHUB_STEP_SUMMARY
          echo "| **📝 نام سرور** | \`${{ github.event.inputs.server_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **🌍 منطقه** | \`${{ github.event.inputs.region }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **💻 پلن** | \`${{ github.event.inputs.size_slug }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **🌐 IPv6** | ${{ github.event.inputs.enable_ipv6 }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **💾 Backups** | ${{ github.event.inputs.enable_backups }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **📊 Monitoring** | ${{ github.event.inputs.enable_monitoring }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **🏷️ Tags** | \`${{ github.event.inputs.custom_tags }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
      # ════════════════════════════════════════════════════════════════════
      # 🏗️ Step 5: ایجاد سرور
      # ════════════════════════════════════════════════════════════════════
      
      - name: 🏗️ Create Server
        id: create_droplet
        env:
          DO_DROPLET_NAME: ${{ github.event.inputs.server_name }}
          DO_REGION: ${{ github.event.inputs.region }}
          DO_SIZE_SLUG: ${{ github.event.inputs.size_slug }}
          DO_ENABLE_IPV6: ${{ github.event.inputs.enable_ipv6 }}
          DO_ENABLE_BACKUPS: ${{ github.event.inputs.enable_backups }}
          DO_TAGS: ${{ github.event.inputs.custom_tags }}
          DO_API_TOKEN: ${{ secrets.DO_API_TOKEN }}
          SSH_KEY_NAME: ${{ secrets.SSH_KEY_NAME }}
        run: |
          echo "🏗️ شروع ایجاد سرور..."
